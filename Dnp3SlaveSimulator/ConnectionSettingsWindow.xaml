<Window x:Class="Dnp3SlaveSimulator.ConnectionSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Connection Settings"
        Width="820"
        Height="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip">
    <Window.Resources>
        <Style x:Key="TcpSectionBorderStyle" TargetType="Border" BasedOn="{StaticResource CardBorderStyle}">
            <Setter Property="IsEnabled" Value="True" />
            <Setter Property="Opacity" Value="1" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsTcpTransport}" Value="False">
                    <Setter Property="IsEnabled" Value="False" />
                    <Setter Property="Opacity" Value="0.55" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="SerialSectionBorderStyle" TargetType="Border" BasedOn="{StaticResource CardBorderStyle}">
            <Setter Property="IsEnabled" Value="True" />
            <Setter Property="Opacity" Value="1" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSerialTransport}" Value="False">
                    <Setter Property="IsEnabled" Value="False" />
                    <Setter Property="Opacity" Value="0.55" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,12">
            <TextBlock Text="Connection Settings"
                       FontSize="18"
                       FontWeight="Bold" />
            <TextBlock Margin="0,6,0,0"
                       Foreground="{StaticResource TextMutedBrush}"
                       Text="Edit transport, endpoint, serial parameters, addressing, retry behavior, and unsolicited communication settings for the current slave session."
                       TextWrapping="Wrap" />
        </StackPanel>

        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Style="{StaticResource CardBorderStyle}"
                        Padding="16"
                        Margin="0,0,0,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="Transport" />
                            <ComboBox ItemsSource="{Binding TransportOptions}"
                                      SelectedItem="{Binding Connection.Transport}" />
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="0,0,10,0">
                            <TextBlock Text="Master Address" />
                            <TextBox Text="{Binding Connection.MasterAddress, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Outstation Address" />
                            <TextBox Text="{Binding Connection.OutstationAddress, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Grid.Row="1"
                        Style="{StaticResource TcpSectionBorderStyle}"
                        Padding="16"
                        Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock FontSize="16"
                                   FontWeight="SemiBold"
                                   Text="TCP Target" />
                        <TextBlock Margin="0,12,0,0" Text="TCP Endpoint" />
                        <TextBox Text="{Binding Connection.Endpoint, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </Border>

                <Border Grid.Row="2"
                        Style="{StaticResource SerialSectionBorderStyle}"
                        Padding="16"
                        Margin="0,0,0,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                   Grid.ColumnSpan="2"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Text="Serial Target" />

                        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,12,10,10">
                            <TextBlock Text="Serial Port" />
                            <Grid Margin="0,4,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <ComboBox Grid.Column="0"
                                          Margin="0,0,8,0"
                                          ItemsSource="{Binding SerialPortOptions}"
                                          SelectedItem="{Binding Connection.SerialPort, UpdateSourceTrigger=PropertyChanged}" />
                                <Button Grid.Column="1"
                                        Width="70"
                                        Command="{Binding RefreshSerialPortsCommand}"
                                        Content="Refresh" />
                            </Grid>
                            <TextBlock Margin="0,6,0,0"
                                       Foreground="{StaticResource TextMutedBrush}"
                                       Text="{Binding SerialPortAvailabilityText}"
                                       TextWrapping="Wrap" />
                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,12,0,10">
                            <TextBlock Text="Baud Rate" />
                            <TextBox Text="{Binding Connection.SerialBaudRate, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>

                        <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,10,10">
                            <TextBlock Text="Data Bits" />
                            <ComboBox ItemsSource="{Binding SerialDataBitOptions}"
                                      SelectedItem="{Binding Connection.SerialDataBits}" />
                        </StackPanel>

                        <StackPanel Grid.Row="2" Grid.Column="1" Margin="0,0,0,10">
                            <TextBlock Text="Stop Bits" />
                            <ComboBox ItemsSource="{Binding SerialStopBitOptions}"
                                      SelectedItem="{Binding Connection.SerialStopBits}" />
                        </StackPanel>

                        <StackPanel Grid.Row="3" Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="Parity" />
                            <ComboBox ItemsSource="{Binding SerialParityOptions}"
                                      SelectedItem="{Binding Connection.SerialParity}" />
                        </StackPanel>

                        <StackPanel Grid.Row="3" Grid.Column="1">
                            <TextBlock Text="Flow Control" />
                            <ComboBox ItemsSource="{Binding SerialFlowControlOptions}"
                                      SelectedItem="{Binding Connection.SerialFlowControl}" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Grid.Row="3"
                        Style="{StaticResource CardBorderStyle}"
                        Padding="16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                   Grid.ColumnSpan="2"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Text="Recovery and Unsolicited" />

                        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,12,10,0">
                            <TextBlock Text="Port Open Retry (s)" />
                            <TextBox Text="{Binding Connection.PortRetrySeconds, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,12,0,0">
                            <CheckBox Margin="0,0,0,8"
                                      IsChecked="{Binding Connection.EnableUnsolicited}"
                                      Content="Enable Unsolicited" />
                            <WrapPanel>
                                <CheckBox Margin="0,0,16,0"
                                          IsChecked="{Binding Connection.UnsolicitedClass1}"
                                          Content="Class 1" />
                                <CheckBox Margin="0,0,16,0"
                                          IsChecked="{Binding Connection.UnsolicitedClass2}"
                                          Content="Class 2" />
                                <CheckBox IsChecked="{Binding Connection.UnsolicitedClass3}"
                                          Content="Class 3" />
                            </WrapPanel>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </ScrollViewer>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Width="90"
                    Margin="0,0,8,0"
                    IsCancel="True"
                    Content="Close" />
            <Button Width="90"
                    Style="{StaticResource PrimaryButtonStyle}"
                    IsDefault="True"
                    Content="Done"
                    Click="DoneButton_Click" />
        </StackPanel>
    </Grid>
</Window>
